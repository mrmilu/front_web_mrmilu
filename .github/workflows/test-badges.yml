name: Test badges

on:
  push:
    branches: [master]

jobs:
  test-coverage:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Setup yarn
        run: npm install -g yarn

      - run: |
          yarn,
          yarn test:badges
